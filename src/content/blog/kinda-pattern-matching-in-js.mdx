---
title: (Kinda) Pattern Matching in JS
---

# (Kinda) Pattern Matching in JS

```ts
const numToFizzBuzz = (num: number) => {
  if (num % 15 === 0) return "FizzBuzz";
  if (num % 3 === 0) return "Fizz";
  if (num % 5 === 0) return "Buzz";
  return `${num}`;
};
```
